# Scheduler

Compatibility note: scheduling is now treated as a kernel-internal concern
rather than a standalone top-level service doc.

Use these docs instead:

- `docs/arch/v1/kernel/kernel.md`
- `docs/arch/v1/kernel/executor.md`
- `docs/arch/v1/dataflow-walk-notes.md`
