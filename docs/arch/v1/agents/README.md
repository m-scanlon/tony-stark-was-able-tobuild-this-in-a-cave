# Agents (Compatibility Note)

The older agent model has been replaced in the v1 architecture revision.

Current equivalents:

- `Skill` is the execution contract: `docs/arch/v1/skill/skill.md`
- `Job` and `Task` live in the kernel execution model:
  `docs/arch/v1/kernel/kernel.md`
- `Memory` and entities live in the graph model:
  `docs/arch/v1/memory/memory-structure.md`
- shard placement is capability-based:
  `docs/arch/v1/native-protocol/shard/capability-model.md`
