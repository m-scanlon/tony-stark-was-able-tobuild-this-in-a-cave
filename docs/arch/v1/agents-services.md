# Agents Services

Compatibility note: the older agent-services model is no longer the canonical
v1 architecture.

Use these docs instead:

- `docs/arch/v1/kernel/kernel.md` for the execution boundary
- `docs/arch/v1/skill/skill.md` for the execution contract
- `docs/arch/v1/native-protocol/shard/capability-model.md` for shard
  capabilities vs skill requirements
- `docs/arch/v1/memory/memory-structure.md` for memory ownership and promotion
