# Lifecycle

Compatibility note: the older end-to-end lifecycle doc has been split across
the current execution, memory, and skill docs.

Use these docs instead:

- `docs/arch/v1/kernel/kernel.md` for request execution flow
- `docs/arch/v1/skill/skill-lifecycle.md` for skill formation and provisioning
- `docs/arch/v1/memory/memory-structure.md` for promotion and commit flow
