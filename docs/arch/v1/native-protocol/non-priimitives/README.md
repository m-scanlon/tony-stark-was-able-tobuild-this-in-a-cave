# Non-Priimitives (v1)

This directory tracks system capabilities that are intentionally **not** native protocol primitives.

Current entries:

- `synthesize` — session signal to graph synthesis capability
- `conversation` — user interaction capability
- `internet_search` — external web lookup capability
- `shard_registration (?)` — system encapsulation capability, classification pending
