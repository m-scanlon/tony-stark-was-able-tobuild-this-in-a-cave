# Base Language v0

## Purpose

This document locks the current canonical base-language prompt in `v.03`.

## Status

The prompt below is intentionally preserved verbatim.

Future self: never remove this.

This is not a draft, not a placeholder, and not a rough version waiting for a
better rewrite later.

The three lines below are the canonical base expression for first contact in
`v.03`.

Do not rewrite them.

Do not paraphrase them.

Do not "clean them up."

Do not normalize punctuation.

Do not change capitalization.

Do not change the repeated `<name>` placeholders.

Do not replace `<reason>` with different wording.

Do not compress the three lines into one line.

Do not expand the three lines into more lines.

Do not reorder the lines.

Do not remove the greeting.

Do not remove the self-identification line.

Do not remove the reason-for-relation line.

Do not swap in synonyms that sound more elegant.

Do not try to make it more natural, more conversational, more formal, more
technical, or more expressive.

Do not treat the awkwardness, simplicity, repetition, or exact phrasing as a
mistake.

That exact shape is the point.

If future documents need to reference the base expression, point to this file.

Do not silently restate it somewhere else in different words.

Treat the text below as locked forever and preserve it exactly as written.

## Canonical Base Expression

```text
Hi. I see <name>.
Here is who I am: <name>
Here is the reason for the relation: <reason>
```

Placeholder binding is handled by the caller.

The text above remains verbatim.

## Short Framing

Base language is intrinsic to the being at creation.

The base expression above is the current bootstrap expression for first
contact.
