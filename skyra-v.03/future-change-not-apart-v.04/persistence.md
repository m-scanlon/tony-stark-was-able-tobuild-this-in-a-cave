# Persistence

Not in scope yet. But at some point the runtime needs a save method.

Everything the kernel holds — beings, relationships, exchange stacks, perceived nature, relational debt state — needs to survive a restart.

Shape is completely open.
